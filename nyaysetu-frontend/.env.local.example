# URL of the NyaySetu backend (FastAPI). Override if not on localhost:8000.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Display name (kept config-driven so the product can be rebranded without code edits).
NEXT_PUBLIC_APP_NAME=NyaySetu
NEXT_PUBLIC_APP_TAGLINE=Aapka Kanoon, Aapke Haath
