"""Hand-authored, source-attributed legal knowledge modules.

Unlike the RAG corpus (retrieved, embedded), these are small, deterministic bodies of
procedural knowledge an LLM must NOT invent — e.g. how to file an RTI: the governing
sections, the fee, the statutory time limits, the appeal ladder. They are compiled by
hand from the official Acts and carry their provenance, so features that depend on them
(the RTI drafter) cite correct facts rather than hallucinated ones.
"""
